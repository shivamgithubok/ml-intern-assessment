# Trigram Language Model

This directory contains the core assignment files for the Trigram Language Model.
## I have completed bothe task

## How to Run inside the cmd
# create the venv file 
  ```
  python -m venv .venv
  ```
  ```
  .venv\scripts\activate
  ```
# run the requirements.txt for dependency installing
  ```
  pip install -r requirements.txt 
  ```
# task first to run 
- run the test_ngram.py
  ```
  pytest trigram-assignment/tests/test_ngram.py  
  
  ```
# second task 
- run the demo.py inside attention
  ```
  cd ml-assignment
  ```
  ```
  python  src\attention\demo.py
  ``` 
    # Instructions on how to run. (filled by you)

## Design Choices

Please document your design choices in the `evaluation.md` file. This should be a 1-page summary of the decisions you made and why you made them.